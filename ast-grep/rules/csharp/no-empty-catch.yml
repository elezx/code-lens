id: no-empty-catch
language: CSharp
severity: warning
message: "Empty catch block swallows the exception — log it, wrap it, or let it propagate."
rule:
  kind: catch_clause
  has:
    stopBy: neighbor
    kind: block
    regex: '^\{\s*\}$'
